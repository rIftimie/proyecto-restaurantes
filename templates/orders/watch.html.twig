{% extends 'base.html.twig' %}

{% block title %}Pedido{% endblock %}

{% block body %}
  <div
    {{ react_component('pages/client/ShowPage' , { 'order': order }) }}>
  </div>

  <script type="application/json" id="mercure-url">
    {{ mercure( 'http://localhost:8000/api/orders/' ~ order.id  )|json_encode(constant('JSON_UNESCAPED_SLASHES') b-or constant('JSON_HEX_TAG'))|raw }}
  </script>
    
{% endblock %}