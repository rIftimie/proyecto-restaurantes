import React from 'react';
import Header from '../../controllers/components/Header/Header';
import NavButton from '../../controllers/components/Buttons/NavButton';

const AdminPages = () => {
  return(
    <div>
      <h1>Admin Panel</h1>
      {/* <Header /> */}
      
      {/* <nav className='mainMenu'>
        <NavButton name='Trabajadores' onClick={() => {}} />
        <NavButton name='Pedidos' onClick={() => {}} />
        <NavButton name='Productos' onClick={() => {}} />
        <NavButton name='Mesas' onClick={() => {}} />
      </nav> */}
      
    </div>
  );

};

export default AdminPages;